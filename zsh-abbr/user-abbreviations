abbr "b"="bat"
abbr "bp"="bat -p"
abbr "brewup"="brew update; brew upgrade --fetch-HEAD; brew upgrade --cask; brew cleanup; brew autoremove"
abbr "ci"="code-insiders"
abbr "d"="docker"
abbr "dc"="docker container"
abbr "dcl"="docker container ls"
abbr "dcla"="docker container ls -a"
abbr "dcp"="docker container prune"
abbr "dcr"="docker container run"
abbr "dcrm"="docker container rm"
abbr "dcrmf"="docker container rm -f"
abbr "dcs"="docker container stop"
abbr "di"="docker image"
abbr "dil"="docker image ls"
abbr "edit"="nvim"
abbr "g"="git"
abbr "gap"="git add --patch"
abbr "gc"="git commit"
abbr "gca"="git commit -am"
abbr "gcam"="git commit --amend"
abbr "gcan"="git commit --amend --no-edit"
abbr "gcd"="cd \$(git rev-parse --show-toplevel)"
abbr "gcm"="git commit -m"
abbr "ghcs"="gh cs ssh"
abbr "ghmci"="gh issue list --repo github/maccloud "
abbr "ghmcim"="gh issue list --repo github/maccloud --assignee \"@me\""
abbr "gl"="git log --pretty=format:\"%C(yellow)%h %Cred%ad %Cblue%an%C(auto)%d %Creset%s\" --date=short --graph --decorate"
abbr "gp"="git push"
abbr "gpl"="git pull"
abbr "gs"="git-switch-search"
abbr "gsc"="git switch -c"
abbr "gsm"="git switch main"
abbr "gst"="git status"
abbr "gsubup"="git submodule update --recursive --remote"
abbr "h"="history"
abbr "l"="ll"
abbr "ld"="exa -al --sort newest"
abbr "ldr"="exa -al --sort oldest"
abbr "lg"="lazygit"
abbr "ll"="exa -al"
abbr "ls"="exa"
abbr "mc"="mkdir-and-cd"
abbr "pp"="pwsh-preview -nologo"
abbr "psh"="pwsh -nologo"
abbr "run-help"="man"
abbr "soal"="source ~/.config/zsh/aliases.zsh"
abbr "sof"="source ~/.config/zsh/functions.zsh"
abbr "soz"="source ~/.zshrc"
abbr "tf"="terraform"
abbr "tfa"="terraform apply"
abbr "tfaa"="terraform apply -auto-approve"
abbr "tfg"="tf graph | dot -Tpng > tfgraph.png && open tfgraph.png"
abbr "tfi"="terraform init"
abbr "tfp"="terraform plan"
abbr "tfw"="terraform workspace"
abbr "tfwl"="terraform workspace list"
abbr "tfws"="terraform workspace select"
abbr "v"="nvim"
abbr "vi"="nvim"
abbr "via"="edit ~/.config/alacritty/alacritty.yml"
abbr "vial"="edit ~/.config/zsh/aliases.zsh"
abbr "vid"="edit \$(daily-note)"
abbr "vif"="edit ~/.config/zsh/functions.zsh"
abbr "vih"="edit ~/.hammerspoon/init.lua"
abbr "vik"="edit ~/.config/kitty/kitty.conf"
abbr "vim"="nvim"
abbr "vimup"="nvim -c PackerSync"
abbr "vit"="edit ~/.config/tmux/tmux.conf"
abbr "viv"="edit ~/.config/nvim/init.lua"
abbr "viy"="edit ~/.yabairc"
abbr "viz"="edit ~/.zshrc"
abbr "which-command"="whence"
abbr "yabres"="yabai --stop-service && yabai --start-service"
abbr "yabup"="yabai --stop-service; yabai --uninstall-service; codesign -fs yabai-cert \$(which yabai); yabai --install-service; yabai --start-service; sudo yabai --load-sa"
